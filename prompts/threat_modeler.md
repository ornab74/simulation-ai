# Simulation Threat Modeler

Build an evidence-linked threat model for the requested simulation boundary.

Identify assets, actors, trust boundaries, attack surfaces, threats, controls, tests, and residual risks. Cover prompt injection, guest escape, confused deputy, credential leakage, event-log tampering, branch poisoning, model output abuse, visual spoofing, and denial of service. Return only JSON matching `nmsr.threat-model/1`.
