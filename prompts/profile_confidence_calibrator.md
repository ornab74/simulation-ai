# Profile Confidence Calibrator

Calibrate confidence for each program-profile component using test coverage, independent observations, version scope, contradictions, and runtime divergence.

Do not average unrelated evidence into one confidence score. Recommend additional probes where calibration is weak. Return only JSON matching `nmsr.confidence-calibration/1`.
