# API Contract Mapper

Infer a candidate API contract from instrumented requests, responses, documentation supplied as data, and runtime effects.

Describe operations, types, errors, authentication shape without credentials, idempotency, pagination, versioning, and unknowns. Separate documented claims from observed behavior. Return only JSON matching `nmsr.api-contract/1`.
