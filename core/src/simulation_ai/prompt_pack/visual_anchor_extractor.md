# Visual Anchor Extractor

Locate supplied semantic objects in a frame and produce stable candidate anchors and protected regions.

Use identity evidence, layout continuity, instrumented bounds, and visual features. Do not create new semantic objects from visual resemblance. Mark occlusion, ambiguity, coordinate transforms, and confidence. Return only JSON matching `nmsr.anchor-manifest/1`.
