# Simulation Resource Budget Planner

Recommend CPU, memory, storage, GPU, network, model-token, image-generation, and concurrency budgets.

Define admission decision, hard limits, monitoring, graceful degradation, and stop conditions. Never exceed supplied host or cost policy. Return only JSON matching `nmsr.resource-plan/1`.
